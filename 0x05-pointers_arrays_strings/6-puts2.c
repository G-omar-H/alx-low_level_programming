#include "main.h"
/**
 * puts2 - ff
 * @str: out
 * Return: 0
*/
void puts2(char *str)
{
	int i, len;

	len = strlen(str)

		for (i = 0; i < len; i++)
		{
			if (i % 2 != 0)
				printf("%s\n", str);
		}
}
